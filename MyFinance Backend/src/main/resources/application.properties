spring.application.name=MyFinance Backend

# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/myfinance?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=myfinance_secret_key_2024_very_long_and_secure_key_for_jwt_token_generation
jwt.expiration=86400000

# Logging Configuration
logging.level.com.myfinance=DEBUG

# Email Configuration (Mailtrap SMTP - Testing Only)
# Sign up at https://mailtrap.io to get your credentials
# Go to Email Testing > Inboxes > SMTP Settings
spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=2525
spring.mail.username=${EMAIL_USERNAME:d1300f7d002dd3}
spring.mail.password=${EMAIL_PASSWORD:e39b36898fede0}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Email Settings
app.email.from=${EMAIL_FROM:MyFinance <test@myfinance.com>}
app.email.enabled=${EMAIL_ENABLED:true}
